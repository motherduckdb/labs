[pytest]
# Own rootdir: the repo pyproject.toml configures xdist options this suite does not use.
addopts = -p no:cacheprovider -ra
markers =
    browser: renders the Dive in headless Chromium (Playwright); DCD_E2E_BROWSER=0 skips
filterwarnings =
    ignore::DeprecationWarning
