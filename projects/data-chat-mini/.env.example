# Required ---------------------------------------------------------------

# A production MotherDuck user-scoped personal access token. The username claim
# is required to create personal guides under users/<username>/.... Data access
# remains read-only because the app exposes only the MCP `query` tool, never
# `query_rw`. A read scaling token is suitable only for a read-only deployment:
# it may not be able to create personal guides.
MOTHERDUCK_TOKEN=

# OpenRouter API key (the LLM is called via OpenRouter's OpenAI-compatible API).
OPENROUTER_API_KEY=

# Optional ---------------------------------------------------------------

# MotherDuck API base. Production is the default. Override only when explicitly
# testing another environment; the token must belong to the same environment.
# MOTHERDUCK_API_URL=https://api.staging.motherduck.com

# LLM model id (OpenRouter). Default google/gemini-3-flash-preview.
# OPENROUTER_MODEL=google/gemini-3-flash-preview

# Pin the OpenRouter upstream provider to stabilize prompt caching.
# OPENROUTER_PROVIDER=Google AI Studio

# Reasoning level on startup: none | minimal | low | medium | high | xhigh.
# Defaults to `none` (raw reasoning is kept out of the UI). Client-read, so the
# NEXT_PUBLIC_ prefix is required and changing it needs a rebuild.
# NEXT_PUBLIC_DEFAULT_THINKING_LEVEL=none

# Master kill switch for the controllog telemetry feed (JSONL on disk).
# NEXT_PUBLIC_DISABLE_LOGGING=1
